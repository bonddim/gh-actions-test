---
name: Main

on:
  push:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          echo ${{ github.event.ref }}
          echo ${{ github.ref }}

      - name:  don't run on tags
        if: "!startsWith(github.ref, 'refs/tags/')"
        run: echo ${{ github.ref }}
