---
name: Release

on:
  push:
    tags:
      - '*'
    branches:
      - 'main'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          git remote -v
          git config user.name
          git config user.email
